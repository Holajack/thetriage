# Skills

- **graphify** (`.claude/skills/graphify/SKILL.md`) - any input to knowledge graph. Trigger: `/graphify`
  When the user types `/graphify`, invoke the Skill tool with `skill: "graphify"` before doing anything else.
- **parallax-placement** (`.claude/skills/parallax-placement/SKILL.md`) - place & verify the 9-layer biome parallax (path/trees/bushes/buddy) when art or sprites change; re-measures geometry and renders app-faithful previews without a device build. Trigger: `/parallax-placement`, or whenever new background/sprite art arrives or a biome renders wrong.
